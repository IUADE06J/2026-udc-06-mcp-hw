# MCP-сервери проєкту (Task A)

> Скопіюйте у `docs/mcp/servers.md` і заповніть. По одному блоку на кожен
> підключений публічний сервер (мінімум 2).

**Хост(и), у якому налаштовано:** <напр. Claude Code / Cursor / VS Code>
**Файл конфігурації:** <напр. `.mcp.json` / `.cursor/mcp.json`>

---

## Сервер 1 — <назва>

| | |
|---|---|
| **Навіщо** | <яку задачу він закриває саме в цьому репо> |
| **Транспорт** | <stdio / remote> |
| **Як запускається** | <команда, напр. `npx -y ...`> |
| **Область доступу (scope)** | <які саме шляхи/ресурси йому видно і ЧОМУ саме стільки> |
| **Секрети** | <немає / `${ENV_VAR}`, значення в `.env`, який у `.gitignore`> |
| **Версія** | <зафіксована / плаваюча — і чому> |

**Які tools він дав агенту:** <перелічіть те, що реально побачили в хості>

**Перевірка, що працює:** <що ви спитали в агента і що він реально викликав>

---

## Сервер 2 — <назва>

<той самий блок>

---

## Що НЕ підключали і чому

<коротко: який сервер розглядали, але свідомо не взяли — напр. надто широкий
доступ, потрібен токен із зайвими правами, незрозуміле авторство. Це теж
інженерне рішення, і воно варте одного рядка.>

## Область доступу — головне

Найважливіший рядок у кожному блоці вище — **scope**. Сервер файлової системи,
якому віддали `/` або весь домашній каталог, — це не «зручно налаштовано», це
дірка. Вкажіть точний шлях, який ви дали, і чому цього достатньо.
