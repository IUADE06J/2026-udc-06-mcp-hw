# Task E (bonus) — оберіть ОДИН шлях

> Скопіюйте у `docs/task-e-bonus.md`, залиште ОДИН розділ, інші видаліть.
> Завдання необов'язкове — краще один шлях повністю, ніж три наполовину.

---

## Шлях 1 — той самий сервер у другому хості

**Хост 1:** <напр. Claude Code> · **Хост 2:** <напр. Cursor>

| | Хост 1 | Хост 2 |
|---|---|---|
| Файл конфігурації | | |
| Чи знадобилось міняти сам сервер | | |
| Tools видно | | |
| Resource видно | | |
| Той самий промпт → та сама поведінка? | | |

**Що довелося адаптувати:** <шлях до конфігу? формат запису? абсолютні шляхи?>

**Висновок:** <чи справді сервер портативний, чи портативний лише протокол, а
конфіг усе одно per-host>

---

## Шлях 2 — дебаг через MCP Inspector

**Що робили:** <запустили Inspector проти свого сервера>

**Що побачили:** <список tools, схеми аргументів, реальний виклик і відповідь>

**Що знайшли й полагодили завдяки цьому:** <напр. схема приймала будь-який
рядок; tool падав на невідомому SKU; опис tool був надто розмитий і агент його
не викликав>

**Скріншот/лог (опційно):** <вставте>

**Висновок:** <що дає Inspector порівняно з «просто спробувати в чаті»>

---

## Шлях 3 — сервер, якому потрібен токен, підключений безпечно

**Сервер:** <напр. GitHub MCP>

**Які права дали токену:** <мінімально необхідні — перелічіть>
**Які свідомо НЕ дали:** <і чому>

**Як саме токен потрапляє в конфіг:** <`${ENV_VAR}`, значення в `.env`,
`.env` у `.gitignore`, у репо лише `.env.example`>

**Доказ, що секрет не в репозиторії:**

```bash
git grep -nE "ghp_[A-Za-z0-9]{20,}|github_pat_[A-Za-z0-9_]{20,}|sk-[A-Za-z0-9]{20,}" || echo "clean"
```

<вставте вивід — має бути `clean`>

**Висновок:** <що змінилось у вашій оцінці ризику після підключення сервера з
реальним доступом до зовнішньої системи>
